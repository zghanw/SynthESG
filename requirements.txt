# Core API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# HTTP clients (Tavily research, logo fetching)
requests>=2.31.0
httpx>=0.27.0

# AWS SDK (for S3 in production)
boto3>=1.34.0

# Infrastructure (CDK — only needed for deployment)
aws-cdk-lib>=2.150.0
constructs>=10.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0